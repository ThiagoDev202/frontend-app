function formateDate(date, format) {

}

function calculateSum(numbers) {
    return numbers.reduce((sum, number) => sum + number, 0);
}

function generateUUID() {

}

export default { formateDate, calculateSum, generateUUID };